# .github/workflows/deploy_dagster.yml

name: CI/CD - Deploy Dagster Pipeline

on:
  push:
    branches:
      - main

env:
  IMAGE_NAME: ghcr.io/${{ github.repository }}:latest

jobs:
  
  # --- TÂCHE 1: CONSTRUIRE L'IMAGE ET LA POUSSER ---
  build-and-push-image:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write 
    steps:
      - name: 1. Cloner le dépôt
        uses: actions/checkout@v3

      - name: 2. Se connecter au GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 3. Construire et Pousser l'image
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ env.IMAGE_NAME }}
          build-args: |
            DAGSTER_REQUIREMENTS_FILE=requirements_dagster.txt

  # --- TÂCHE 2: DÉPLOYER L'IMAGE SUR LE SERVEUR ---
  deploy-to-droplet:
    needs: build-and-push-image
    runs-on: ubuntu-latest

    steps:
      - name: 1. Déployer sur le serveur DigitalOcean
        uses: appleboy/ssh-action@v1.0.3
        
        # --- CORRECTION ---
        # L'environnement est défini ICI, au niveau de l'étape...
        env:
          ENV_FILE_PAYLOAD: ${{ secrets.ENV_FILE_CONTENT }}
          DEPLOY_IMAGE_NAME: ${{ env.IMAGE_NAME }}
        
        # ... et "with:" contient les arguments de l'action
        with:
          host: ${{ secrets.DO_HOST }}
          username: ${{ secrets.DO_USER }}
          key: ${{ secrets.DO_SSH_KEY }}
          script_stop: true # Arrête le script si une commande échoue
          
          # Le script à exécuter sur le serveur
          script: |
            echo "✅ 1/5 - Connexion SSH réussie"
            
            # On définit le chemin absolu et on s'y place
            export DEPLOY_DIR="/root/anime-data-platform"
            cd $DEPLOY_DIR
            
            echo "🔑 2/5 - Création du fichier .env..."
            # Les variables ($ENV_FILE_PAYLOAD) sont maintenant disponibles
            echo "$ENV_FILE_PAYLOAD" > $DEPLOY_DIR/.env
            echo "✅ Fichier .env créé."

            echo "🐳 3/5 - Pull de la nouvelle image: $DEPLOY_IMAGE_NAME"
            docker pull $DEPLOY_IMAGE_NAME
            
            echo "🛑 4/5 - Arrêt de l'ancien conteneur dagster_app"
            docker rm -f dagster_app || true
            
            echo "🚀 5/5 - Démarrage du nouveau conteneur !"
            docker run -d -p 3000:3000 \
              --env-file $DEPLOY_DIR/.env \
              -v dagster_storage:/app/dagster_home \
              --name dagster_app \
              --restart always \
              $DEPLOY_IMAGE_NAME
            
            echo "🎉 Déploiement terminé !"